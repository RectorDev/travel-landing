import type { Metadata } from "next";
import { Noto_Sans_Arabic, Vazirmatn } from "next/font/google";
import Navbar from "@/components/Navbar";
import "./globals.css";
import Footer from "@/components/Footer";
import ThemeToggle from "@/components/ThemeToggle";

const vazir = Vazirmatn({ variable: "--font-vazir", subsets: ["arabic", "latin"] });
const noto = Noto_Sans_Arabic({ variable: "--font-noto", subsets: ["arabic"] });
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`antialiased ${vazir.variable} ${noto.variable} font-noto duration-150 transition-colors`}>
      <div className="fixed right-5 bottom-10 z-50"><ThemeToggle/></div>
        <header>
          <Navbar />
        </header>
        <main className="relative overflow-hidden"> {children}</main>
        <footer>
          <Footer />
        </footer>
      </body>
    </html>
  );
}
